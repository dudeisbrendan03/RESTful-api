{
  "_comment": "This is only meant to be utilized for testing!",
  "name": "RESTful",
  "private": false,
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "author": {
    "name": "",
    "email": ""
  },
  "dependencies": {
    "mocha": "^3.0.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-mocha": "^2.2.0",
    "mocha-junit-reporter": "^1.12.0", 
    "gulp-istanbul": "^1.1.3", 
    "nyc": "^12.0.2",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "supertest": "^1.2.0"
  },
  "scripts": {
    "test": "nyc --reporter=cobertura --reporter=html ./node_modules/.bin/mocha tests/**/*.js --reporter mocha-junit-reporter --reporter-options mochaFile=./TEST-RESULTS.xml"
  }
}
